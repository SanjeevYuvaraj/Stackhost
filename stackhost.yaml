user:
  name: YUVI
  github: SanjeevYuvaraj

language: "python"
python_version: "3.10"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "nano inxi"
  start:
    - "python3 -m stackhost"

env:
  - "ENVIRONMENT=production"
